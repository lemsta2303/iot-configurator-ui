import { Outlet } from 'react-router-dom';

/**
 * AddDevicePage component that serves as the main entry point for adding devices.
 */
function AddDevicePage() {
  return <Outlet />;
}

export default AddDevicePage;
